services:
  nginx:
    volumes:
      - ./nginx/default-ssl.conf:/etc/nginx/conf.d/default.conf:ro
      - ./ssl:/etc/nginx/ssl:ro

  app:
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
